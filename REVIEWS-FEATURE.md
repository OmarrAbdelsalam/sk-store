# ميزة المراجعات (Reviews Feature)

## الوصف
تم إضافة ميزة المراجعات التي تسمح للعملاء بتقييم المنتجات بعد استلام الطلب.

## الملفات المضافة

### 1. المكونات (Components)
- `src/components/orders/OrderItemReview.tsx` - مكون عرض وحذف المراجعة
- `src/components/orders/ReviewDialog.tsx` - نافذة إضافة المراجعة

### 2. الترجمات
تم إضافة قسم "Reviews" في:
- `messages/ar.json`
- `messages/en.json`

### 3. التحديثات
- `src/app/[locale]/my-orders/page.tsx` - تم تحديث صفحة الطلبات لعرض المراجعات

## الميزات

### إضافة مراجعة
- يظهر زر "إضافة تقييم" لكل منتج في الطلبات المكتملة (delivered)
- تقييم من 1 إلى 5 نجوم
- تعليق اختياري (حتى 500 حرف)

### عرض المراجعة
- عرض التقييم والتعليق للمنتجات التي تم تقييمها
- عرض النجوم بشكل مرئي

### حذف المراجعة
- إمكانية حذف المراجعة مع تأكيد
- رسالة تحذير قبل الحذف

## API Endpoints المستخدمة

### POST /api/Reviews
إضافة مراجعة جديدة
```json
{
  "orderItemId": 0,
  "sessionId": "string",
  "rating": 5,
  "comment": "string"
}
```

### DELETE /api/Reviews/{reviewId}
حذف مراجعة موجودة
```
Query Parameters:
- sessionId: string
```

## الشروط
- المراجعات تظهر فقط للطلبات ذات الحالة "delivered" (تم التوصيل)
- كل منتج في الطلب يمكن تقييمه بشكل منفصل
- يمكن حذف المراجعة وإضافة مراجعة جديدة

## التصميم
- تصميم متجاوب يعمل على جميع الأحجام
- دعم كامل للغة العربية (RTL) والإنجليزية (LTR)
- استخدام مكونات shadcn/ui للواجهة
